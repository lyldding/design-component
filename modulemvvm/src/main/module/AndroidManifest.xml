<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.lyldding.modulemvvm">

    <application
        android:name="debug.ModuleApplication"
        android:allowBackup="true"
        android:icon="@mipmap/mvvm_ic_launcher"
        android:label="@string/mvvm_app_name"
        android:roundIcon="@mipmap/mvvm_ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MvvmMainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>